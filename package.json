{
  "name": "pomodoro-timer",
  "version": "2.0.0",
  "main": "main.js",
  "build": {
    "appId": "com.jovylle.pomodoro",
    "mac": {
      "target": "dmg",
      "icon": "icon.icns"
    },
    "files": [
      "main.js",
      "renderer.js",
      "index.html",
      "package.json",
      "assets/**"
    ]
  },
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder",
    "build:clean": "rm -rf dist && electron-builder",
    "release:patch": "npm version patch && npm run dist",
    "release:minor": "npm version minor && npm run dist",
    "release:major": "npm version major && npm run dist",
    "release:no-tag-patch": "npm version patch --no-git-tag-version && npm run dist"
  },
  "devDependencies": {
    "electron": "^28.1.0",
    "electron-builder": "^24.13.3"
  }
}

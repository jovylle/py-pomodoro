{
  "name": "pomodoro-timer",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . PomodoroTimer --platform=darwin --arch=x64 --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^28.1.0",
    "electron-packager": "^17.1.2"
  }
}